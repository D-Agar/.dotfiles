return {
  "folke/snacks.nvim",
  opts = {
    picker = {
      sources = {
        explorer = {
          hidden = true, -- Shows hidden files by default
          -- ignored = true, -- Uncomment this if you also want to show git-ignored files by default
        },
      },
    },
  },
}
